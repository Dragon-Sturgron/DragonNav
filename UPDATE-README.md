DragonNav V4.2 - Logo Only 搜索引擎下拉优化

本次修改：
- 搜索框左侧默认只显示当前搜索引擎 Logo。
- 不再显示当前搜索引擎名称。
- 取消浏览器原生 select 下拉框。
- 使用自定义下拉菜单。
- 下拉菜单显示 Logo + 搜索引擎名称。
- 当前搜索引擎显示选中标记。
- 支持深色模式、移动端和点击空白关闭。
- 搜索引擎数据、图标和排序继续来自 KV / 后台搜索引擎管理。

需要覆盖：
- index.html

需要新增：无
需要删除：无
其他文件无需修改。
