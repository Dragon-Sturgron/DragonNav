DragonNav V4.8 - 搜索引擎 Logo 按钮边框跟随背景色

本次仅优化前台样式：
- 搜索框左侧 Logo 按钮增加显式边框。
- 边框颜色会根据“按钮默认背景色”自动变化。
- 鼠标悬停时，边框颜色也会随“按钮悬停背景色”一起变化。
- Logo 圆形底板的细边框也会随按钮默认背景色变化。

这样当你在后台修改：
- 搜索引擎按钮默认背景色
- 搜索引擎按钮悬停背景色

左侧红框位置看到的边框/轮廓也会自然跟着变，不会一直是固定灰色。

本次文件操作：
需要覆盖：
- index.html

需要新增：无
需要删除：无
其他文件无需修改。
