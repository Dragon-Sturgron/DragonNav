DragonNav V4.7 - 搜索引擎按钮背景颜色自定义

这次明确把颜色作用对象改为“按钮本身”。

后台首页设置新增/恢复三个颜色：

1. 搜索引擎按钮默认背景色
   - 控制未选中的搜索引擎按钮
   - 也控制搜索框左侧只显示 Logo 的按钮默认背景

2. 搜索引擎按钮悬停背景色
   - 鼠标放到按钮上时使用

3. 搜索引擎按钮选中背景色
   - 当前选中的搜索引擎使用

默认值：
- 默认按钮：#ffffff
- Hover：#f1f2f4
- 选中：#52525b

下拉菜单容器本身保持白色，不受这三个按钮颜色影响。

三个颜色修改后均自动同步到 KV。

本次文件操作：

需要覆盖：
- index.html
- edge-functions/[path].js
- edge-functions/api/config.js

需要新增：无
需要删除：无

无需修改：
- edge-functions/api/login.js

现有 NAV_CONFIG 无需删除。
